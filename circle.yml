dependencies:
  cache_directories:
    - ~/.m2
    - ~/.ivy2
    - ~/.sbt

  override:
    - "./tools/activator/activator update"

test:
  pre:
    - cd webapp

  override:
    - "./tools/activator/activator clean test"


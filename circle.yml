notify:
  webhooks:
    # gitter hook
    - url: https://webhooks.gitter.im/e/d03f8e48b074a36a94e0
dependencies:
  cache_directories:
    - ~/.m2
    - ~/.ivy2
    - ~/.sbt
  override:
    - cd webapi && ./activator update
test:
  override:
    - cd webapi && ./activator clean test

